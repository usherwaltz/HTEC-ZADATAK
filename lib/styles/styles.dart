import 'package:flutter/material.dart';

class Styles {
  static const tournamentCardTitle = TextStyle(
    fontSize: 25
  );

  static const tournamentCardPadding = EdgeInsets.only(
    left: 10.0,
    right: 10.0,
    top: 5,
    bottom: 5,
  );
}